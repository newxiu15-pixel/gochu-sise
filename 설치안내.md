# 설치 안내

## 1단계 — 지금 바로 확인하기 (내 컴퓨터에서)

브라우저 주소창에 입력

```
http://127.0.0.1:8899
```

수집한 504일치 시세로 화면이 그대로 뜬다.
품종 버튼과 기간 버튼을 눌러보고, 글씨 크기가 적당한지 본다.

이 주소는 이 컴퓨터에서만 열린다. 아버지 폰에서 보려면 2단계가 필요하다.

---

## 2단계 — 인터넷에 올리기 (한 번만)

### GitHub 저장소 만들기

1. [github.com](https://github.com) 로그인 (계정이 없으면 무료 가입)
2. 오른쪽 위 **+** → **New repository**
3. 이름은 `gochu-sise`, **Private**로 둬도 화면은 공개된다
4. **Create repository**

### 코드 올리기

`C:\Users\user\projects\gochu-sise` 폴더에서

```bash
git init
git add .
git commit -m "고추 시세 첫 등록"
git branch -M main
git remote add origin https://github.com/내아이디/gochu-sise.git
git push -u origin main
```

### 화면 켜기

저장소 페이지에서

1. **Settings** → 왼쪽 **Pages**
2. Source: **Deploy from a branch**
3. Branch: **main**, 폴더는 **/docs** 선택 → **Save**
4. 1분쯤 뒤 주소가 뜬다

```
https://내아이디.github.io/gochu-sise/
```

### 자동 수집 켜기

1. **Settings** → **Actions** → **General**
2. Workflow permissions에서 **Read and write permissions** 선택 → **Save**

이제 평일 오후 6시와 밤 9시에 알아서 시세를 가져온다.
**Actions** 탭에서 `Run workflow`를 눌러 지금 한 번 돌려보면 정상 동작을 바로 확인할 수 있다.

---

## 3단계 — 아버지 폰에 깔기

폰에서 위 주소를 연 다음

**안드로이드 (삼성 인터넷 / 크롬)**
1. 오른쪽 위 **⋮** 메뉴
2. **홈 화면에 추가**
3. 이름을 `고추시세`로 두고 **추가**

**아이폰 (사파리)**
1. 아래쪽 가운데 **공유** 버튼 (□에 화살표)
2. **홈 화면에 추가**
3. 이름을 `고추시세`로 두고 **추가**

바탕화면에 아이콘이 생긴다. 누르면 앱처럼 바로 열린다.
설치가 아니라 링크라서 용량을 먹지 않고, 업데이트할 것도 없다.

---

---

## 아버지께 미리 말씀드릴 것

**화면 맨 위에 빨간 글씨가 뜨면 알려달라고 해두세요.**

정상일 때는 날짜 아래에 회색으로 이렇게만 뜬다.

```
자동 확인 정상 · 오늘 21:22 확인함
```

문제가 생기면 맨 위에 빨간 칸이 생기고, 안에 이렇게 적힌다.

```
오류 발생 — 프로그램 수정이 필요합니다
이 화면을 아들에게 보여주세요
```

아버지는 그 화면을 캡처해서 보내주시기만 하면 된다.
아래 회색 작은 글씨에 원인이 적혀 있어 어디를 고쳐야 하는지 바로 알 수 있다.

노란 칸은 고장이 아니다. 공휴일이라 경매가 없었다는 뜻이니 그냥 두면 된다.

---

## 알아두면 좋은 것

**시세는 언제 갱신되나**
서안동 경매는 평일 오전이고, 시세표는 보통 오후에 올라온다.
프로그램은 오후 6시와 밤 9시에 확인하니, 저녁에 보면 그날 시세가 들어와 있다.

**주말과 공휴일**
경매가 없으므로 마지막 경매일 시세가 그대로 보인다.
사흘 넘게 새 시세가 없으면 화면 맨 위에 노란 경고가 뜬다.

**인터넷이 안 되면**
"시세를 불러오지 못했습니다"라고 뜬다. 연결 후 새로고침하면 된다.

**요금**
전부 무료다. GitHub 무료 계정으로 충분하고 초과 요금이 붙는 구조가 아니다.
